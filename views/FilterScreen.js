import React from 'react';
import { View, Text } from 'react-native';

export default (FilterScreen = (props) => {
	return (
		<View>
			<Text>Filter</Text>
		</View>
	);
});

FilterScreen.navigationOptions = (data) => {

    
}